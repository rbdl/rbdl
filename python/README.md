Experimental Python wrapper for RBDL based on Cython
====================================================

This code requires C++11 features and must be compiled with the flags
```-std=c++11``` (or on older compilers: ```-std=c++0x```).
